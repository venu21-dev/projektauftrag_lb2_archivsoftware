<Window x:Class="archivsoftware.MoveFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ordner verschieben" 
        Height="450" 
        Width="450"
        WindowStartupLocation="CenterOwner"
        Background="#1A1A1A"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titel -->
        <TextBlock Grid.Row="0"
                   Text="Ordner verschieben"
                   Foreground="#E0E0E0"
                   FontSize="16"
                   FontWeight="SemiBold"
                   Margin="0,0,0,15"/>

        <!-- Info Text -->
        <TextBlock Grid.Row="1"
                   Text="Wählen Sie den Ziel-Ordner aus:"
                   Foreground="#A0A0A0"
                   FontSize="13"
                   Margin="0,0,0,12"/>

        <!-- TreeView für Ordnerauswahl -->
        <Border Grid.Row="2"
                Background="#0D0D0D"
                BorderBrush="#404040"
                BorderThickness="1"
                CornerRadius="6"
                Padding="10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Option: Root-Ebene -->
                    <Border Background="#1A1A1A"
                            BorderBrush="#2A2A2A"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="12"
                            Margin="0,0,0,8"
                            Cursor="Hand"
                            MouseLeftButtonDown="RootOption_Click">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#242424"/>
                                        <Setter Property="BorderBrush" Value="#3A3A3A"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📁" 
                                       FontSize="16" 
                                       Margin="0,0,10,0"
                                       VerticalAlignment="Center"/>
                            <TextBlock Text="[Root-Ebene]" 
                                       Foreground="#E0E0E0"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Alle Ordner -->
                    <ItemsControl x:Name="FoldersListView"
                                  ItemsSource="{Binding}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="0,0,0,5">
                                    <!-- Haupt-Ordner -->
                                    <Border Background="#1A1A1A"
                                            BorderBrush="#2A2A2A"
                                            BorderThickness="1"
                                            CornerRadius="6"
                                            Padding="12"
                                            Margin="0,0,0,3"
                                            Cursor="Hand"
                                            Tag="{Binding Id}"
                                            MouseLeftButtonDown="FolderBorder_Click">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="#242424"/>
                                                        <Setter Property="BorderBrush" Value="#3A3A3A"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📁" 
                                                       FontSize="16" 
                                                       Margin="0,0,10,0"
                                                       VerticalAlignment="Center"/>
                                            <TextBlock Text="{Binding DisplayName}" 
                                                       Foreground="#E0E0E0"
                                                       FontSize="14"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Border>

                                    <!-- Unterordner (eingerückt) -->
                                    <ItemsControl ItemsSource="{Binding Children}"
                                                  Margin="20,0,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="#1A1A1A"
                                                        BorderBrush="#2A2A2A"
                                                        BorderThickness="1"
                                                        CornerRadius="6"
                                                        Padding="12"
                                                        Margin="0,0,0,3"
                                                        Cursor="Hand"
                                                        Tag="{Binding Id}"
                                                        MouseLeftButtonDown="FolderBorder_Click">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="#242424"/>
                                                                    <Setter Property="BorderBrush" Value="#3A3A3A"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📁" 
                                                                   FontSize="14" 
                                                                   Margin="0,0,8,0"
                                                                   VerticalAlignment="Center"/>
                                                        <TextBlock Text="{Binding DisplayName}" 
                                                                   Foreground="#E0E0E0"
                                                                   FontSize="13"
                                                                   VerticalAlignment="Center"/>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,15,0,0">

            <!-- Abbrechen Button -->
            <Button Content="Abbrechen"
                    Width="100"
                    Height="35"
                    Background="#0D0D0D"
                    Foreground="#E0E0E0"
                    BorderBrush="#404040"
                    BorderThickness="1"
                    Margin="0,0,10,0"
                    Click="CancelButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            CornerRadius="6">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                        VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1A1A1A"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>